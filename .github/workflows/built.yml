name: Build APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

# Directs GitHub Actions to execute commands inside the project subfolder
defaults:
  run:
    working-directory: FlashDance

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build debug APK
        run: ./gradlew assembleDebug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: flash-dance-debug-apk
          # We specify the subfolder path so the runner finds the compiled binary
          path: FlashDance/app/build/outputs/apk/debug/app-debug.apk
